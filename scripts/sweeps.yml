# place your sweep configurations here
program: train.py
method: random

metric:
  name: validation/loss
  goal: minimize

parameters:
  learning_rate:
    min: 0.0001
    max: 0.1
  batch_size:
    values: [2,4,8] # [16, 32, 64]
  epochs:
    values: [5, 10, 15]
  # optimizer:
  #   values: ["adam", "sgd"]

  
